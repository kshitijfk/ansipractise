---
- name: grant tomcat ownership on the user
  execute: sudo chown -R tomcat:tomcat /opt/tomcat/

- name: change to execute permission on user level on bin folder
  execute: sudo chmod -R u+x /opt/tomcat/bin